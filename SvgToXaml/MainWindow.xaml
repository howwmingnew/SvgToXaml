<Window
    x:Class="SvgToXaml.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:explorer="clr-namespace:SvgToXaml.Explorer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:SvgToXaml.ViewModels"
    xmlns:wrapPanel="clr-namespace:SvgToXaml.WrapPanel"
    Title="SvgToXaml"
    d:DataContext="{x:Static viewModels:SvgImagesViewModel.DesignInstance}"
    AllowDrop="True"
    Background="#FF1E1E1E"
    Drop="MainWindow_OnDrop"
    FontFamily="Segoe UI"
    Foreground="#FFCCCCCC"
    Icon="icon.ico"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <!-- Export Icon -->
        <DrawingImage x:Key="SaveDrawingImage">
            <DrawingImage.Drawing>
                <DrawingGroup ClipGeometry="M0,0 V512 H512 V0 H0 Z">
                    <GeometryDrawing Brush="#FFCCCCCC" Geometry="F1 M512,512z M0,0z M146.365,243.417L196.529,243.417 196.529,200.195 315.468,200.195 315.468,243.417 365.634,243.417 255.999,353.243 146.365,243.417z M196.529,123.862L315.468,123.862 315.468,93.028 196.529,93.028 196.529,123.862z M345.469,85.095L345.469,131.409C388.036,160.522 414.93,208.679 414.93,262.805 414.93,305.258 398.399,345.168 368.381,375.186 338.363,405.204 298.452,421.735 256,421.735 213.549,421.735 173.638,405.204 143.62,375.186 113.601,345.168 97.07,305.258 97.07,262.805 97.07,208.805 123.864,160.59 166.529,131.41L166.529,85.095C101.604,117.849 57.07,185.122 57.07,262.806 57.07,372.673 146.134,461.736 256,461.736 365.867,461.736 454.93,372.674 454.93,262.806 454.93,185.12 410.396,117.847 345.469,85.095z M315.469,180.195L315.469,143.862 196.529,143.862 196.529,180.195 315.469,180.195z M315.469,73.028L315.469,50.265 196.529,50.265 196.529,73.029 315.469,73.029z" />
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
        <!-- Folder Open Icon -->
        <DrawingImage x:Key="FolderOpenDrawingImage">
            <DrawingImage.Drawing>
                <GeometryDrawing Brush="#FFCCCCCC" Geometry="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z" />
            </DrawingImage.Drawing>
        </DrawingImage>
        <!-- Settings Gear Icon -->
        <DrawingImage x:Key="SettingsDrawingImage">
            <DrawingImage.Drawing>
                <GeometryDrawing Brush="#FFCCCCCC" Geometry="M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.67 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" />
            </DrawingImage.Drawing>
        </DrawingImage>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="160" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ========== TOOLBAR ========== -->
        <Border
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Style="{StaticResource ToolBarPanelStyle}">
            <DockPanel>
                <!-- Right-side controls -->
                <!-- Update Button（有新版本時才顯示） -->
                <Button
                    Margin="3,0"
                    Padding="8,2"
                    Background="#FF0E639C"
                    BorderBrush="#FF1177BB"
                    BorderThickness="1"
                    Command="{Binding OpenUpdatePageCommand}"
                    Content="{Binding UpdateButtonText}"
                    DockPanel.Dock="Right"
                    FontSize="11"
                    FontWeight="SemiBold"
                    Foreground="White"
                    ToolTip="{Binding UpdateTooltip}"
                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3"
                                            Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#FF1177BB" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#FF0A4D7A" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <Rectangle
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarSeparatorStyle}"
                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Margin="3,0"
                    Command="{Binding ToggleLanguageCommand}"
                    Content="{Binding LanguageLabel}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="{DynamicResource S.Tooltip.ToggleLang}" />
                <Button
                    Margin="3,0"
                    Command="{Binding OpenSettingsCommand}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="{DynamicResource S.Tooltip.Settings}">
                    <Image Width="18" Height="18" Source="{StaticResource SettingsDrawingImage}" />
                </Button>
                <Rectangle DockPanel.Dock="Right" Style="{StaticResource ToolBarSeparatorStyle}" />
                <Button
                    Margin="3,0"
                    Command="{Binding ToggleBackgroundCommand}"
                    Content="{Binding BackgroundLabel}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="{DynamicResource S.Tooltip.ToggleBg}" />
                <Slider
                    x:Name="Slider"
                    Width="110"
                    Margin="6,0"
                    VerticalAlignment="Center"
                    DockPanel.Dock="Right"
                    LargeChange="8"
                    Maximum="500"
                    Minimum="16"
                    Orientation="Horizontal"
                    SmallChange="2"
                    ToolTip="{DynamicResource S.Tooltip.IconSize}"
                    Value="{Binding IconSize}" />
                <Rectangle DockPanel.Dock="Right" Style="{StaticResource ToolBarSeparatorStyle}" />
                <Button
                    Margin="3,0"
                    Command="{Binding ExportDirCommand}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="{DynamicResource S.Tooltip.Export}">
                    <Image Width="18" Height="18" Source="{StaticResource SaveDrawingImage}" />
                </Button>
                <Button
                    Margin="3,0"
                    Command="{Binding OpenFolderCommand}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="{DynamicResource S.Tooltip.OpenFolder}">
                    <Image Width="18" Height="18" Source="{StaticResource FolderOpenDrawingImage}" />
                </Button>
                <Rectangle DockPanel.Dock="Right" Style="{StaticResource ToolBarSeparatorStyle}" />
                <!-- Address bar fills remaining space -->
                <TextBox
                    Margin="2,0"
                    Style="{StaticResource AddressBarStyle}"
                    TabIndex="1"
                    Text="{Binding CurrentDir, Mode=TwoWay}" />
            </DockPanel>
        </Border>

        <!-- ========== LEFT: FOLDER TREE ========== -->
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Background="#FF252526">
            <explorer:FolderTree
                x:Name="FolderTree"
                ContextMenuCommands="{Binding ContextMenuCommands}"
                CurrentFolder="{Binding CurrentDir, Mode=TwoWay}"
                TabIndex="3" />
        </Border>

        <!-- ========== GRID SPLITTER ========== -->
        <GridSplitter
            Grid.Row="1"
            Grid.Column="1"
            Width="4"
            ResizeBehavior="PreviousAndNext"
            ResizeDirection="Columns"
            Style="{StaticResource DarkGridSplitterStyle}" />

        <!-- ========== RIGHT: ICON LIST + STATUS BAR ========== -->
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Icon ListBox -->
            <ListBox
                Grid.Row="0"
                Background="{Binding PreviewBackgroundBrush}"
                BorderThickness="0"
                ItemsSource="{Binding Images}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                SelectionMode="Single">
                <!-- Explicit template: standard ScrollViewer with dark scrollbars -->
                <ListBox.Template>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="False"
                                          Padding="{TemplateBinding Padding}"
                                          Style="{StaticResource DarkScrollViewerStyle}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <wrapPanel:VirtualizingWrapPanel ItemHeight="{Binding ElementName=Slider, Path=Value}" ItemWidth="{Binding ElementName=Slider, Path=Value}" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="3" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border
                                        x:Name="ItemBorder"
                                        Background="#22FFFFFF"
                                        BorderBrush="#18FFFFFF"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="0"
                                        SnapsToDevicePixels="True">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#28FFFFFF" />
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#30FFFFFF" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#40264F78" />
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#FF3794FF" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:ImageBaseViewModel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <!-- SVG Icon -->
                            <!-- 單擊：複製 Button Style XAML（預設輸出格式） -->
                            <!-- 雙擊：保留同一指令（避免快速雙擊使用者反而沒效果） -->
                            <Image
                                Grid.Row="0"
                                Margin="6,6,6,2"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Source="{Binding PreviewSource}">
                                <Image.InputBindings>
                                    <MouseBinding Command="{Binding CopyXamlCommand}" Gesture="LeftClick" />
                                    <MouseBinding Command="{Binding CopyXamlCommand}" Gesture="LeftDoubleClick" />
                                </Image.InputBindings>
                            </Image>
                            <!-- Complex SVG badge -->
                            <TextBlock
                                x:Name="ComplexBadge"
                                Grid.Row="0"
                                Margin="4,2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                FontSize="9"
                                FontWeight="Bold"
                                Foreground="Orange"
                                Text="DI"
                                ToolTip="{DynamicResource S.Tooltip.ComplexSvg}"
                                Visibility="{Binding IsComplexSvg, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!-- Filename label -->
                            <Border Grid.Row="1"
                                    Margin="0"
                                    Padding="3,2"
                                    CornerRadius="0,0,5,5"
                                    Background="#60000000">
                                <TextBlock
                                    FontSize="11"
                                    Foreground="#FFDDDDDD"
                                    Text="{Binding Filename}"
                                    TextAlignment="Center"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip="{Binding Filename}" />
                            </Border>
                            <!-- Context Menu -->
                            <Grid.ContextMenu>
                                <ContextMenu Style="{StaticResource DarkContextMenuStyle}">
                                    <MenuItem
                                        Command="{Binding CopyXamlCommand}"
                                        Header="{DynamicResource S.Menu.CopyXaml}"
                                        Style="{StaticResource DarkMenuItemStyle}" />
                                    <MenuItem
                                        Command="{Binding OpenDetailCommand}"
                                        Header="{DynamicResource S.Menu.ViewDetail}"
                                        Style="{StaticResource DarkMenuItemStyle}" />
                                    <MenuItem
                                        Command="{Binding OpenFileCommand}"
                                        Header="{DynamicResource S.Menu.OpenFile}"
                                        Style="{StaticResource DarkMenuItemStyle}" />
                                </ContextMenu>
                            </Grid.ContextMenu>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding HasSvg}" Value="false">
                                <Setter TargetName="ComplexBadge" Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Toast Notification -->
            <Border
                x:Name="ToastBorder"
                Grid.Row="0"
                Margin="0,0,0,50"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                IsHitTestVisible="False"
                Opacity="0"
                Panel.ZIndex="10"
                Style="{StaticResource ToastBorderStyle}">
                <TextBlock
                    FontSize="14"
                    Foreground="White"
                    Text="{DynamicResource S.Toast.Copied}" />
            </Border>

            <!-- Status Bar -->
            <Border
                Grid.Row="1"
                Style="{StaticResource StatusBarPanelStyle}">
                <DockPanel>
                    <Button
                        Margin="2,0"
                        Command="{Binding OpenFileCommand}"
                        DockPanel.Dock="Right"
                        Style="{StaticResource StatusBarButtonStyle}"
                        ToolTip="{DynamicResource S.Tooltip.OpenFile}">
                        <TextBlock FontSize="11" Foreground="White" Text="..." />
                    </Button>
                    <TextBox
                        Margin="2,0"
                        Style="{StaticResource StatusBarTextStyle}"
                        Text="{Binding SelectedItem.Filepath, Mode=OneWay}" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
